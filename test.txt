{
    idup
    3 i% 0 i= iswap
    5 i% 0 i= iswap
    { 'Fizz }
    { ' } iifelse
    { 'Buzz }
    { ' } iifelse
    idup
    concat sdup
    { sdrop idup itos } s!if
}
'fizzbuzz-replace define

{
    20 'limit define
    0 idup limit i<
    {
        idup fizzbuzz-replace s.
        1 i+ idup limit i<
    }
    iwhile
}
'fizzbuzz define

{
    fizzbuzz
}
'main define

